# Security Policy

本文件说明 CodeLab 的安全基线与凭据处理流程。实验室制度层面的保密要求、合作企业数据管理责任以飞书 CodeLab 成员手册为准。

## 这是学习型开源组织

CodeLab 存在的意义就是让成员在真实项目里学会工程实践，所以安全问题上不搞"私下报告"那一套：

- **代码漏洞、依赖告警、配置隐患，欢迎直接开 Issue 公开讨论。** 公开讨论本身就是学习过程，别人也能从中学到东西。
- 不确定是不是安全问题的，直接问，不要因为"这可能是安全问题"就不敢提。

唯一需要区别对待的是**凭据**。原因不是要保密，而是泄露已经发生了：密钥一旦提交进 Git，即使之后删除，历史里依然存在，并且可能已经被抓取。所以它的处理顺序和普通问题不一样。

## 安全红线

- 不提交密码、API Key、Token、SSH Key、证书私钥
- 不提交真实数据库账号与连接串
- 不提交真实 `.env`，只提交 `.env.example`，且使用空值或占位符
- 不在日志、截图、示例代码中夹带真实密钥
- 不提交合作企业或实验室的内部数据（这条有真实的对外保密义务，不只是习惯问题）

## 误提交凭据怎么办

按这个顺序处理：

1. **先在密钥管理后台轮换或吊销该密钥。** 这是唯一能真正止损的一步；只要它进过 Git 历史，就必须当作已泄露。
2. 开 Issue 说明情况：泄露了什么、什么时候轮换的、需要谁协助清理历史。不需要隐瞒，说清楚反而解决得更快。
3. 清理 Git 历史，并确认旧凭据已彻底失效。
4. 需要协助时联系 `codelab-admin`。

## 涉及合作项目时

企业合作、竞赛等涉密项目的漏洞与数据问题，按合作协议约定的渠道处理；协议没有约定的，先联系 `codelab-admin`。这是履行对外义务，不是组织内部的信息管控。

## 响应预期

| 情形 | 预期响应 |
| --- | --- |
| 已泄露凭据（在 Issue 中说明） | 24 小时内协助轮换与清理 |
| 其他安全问题（公开 Issue） | 3 个工作日内回复 |
